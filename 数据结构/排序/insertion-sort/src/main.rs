fn _insertion_sort(v: &mut [i32]) {
    for i in 1..v.len() - 1 {
        for j in 0..i {
            if v[j] > v[i] {}
        }
    }
}
fn main() {
    //let mut a = [9, 8, 7, 6, 5, 4, 3, 2, 1];
    //insertion_sort(&mut a);
}
